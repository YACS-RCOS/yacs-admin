 <%=render partial: "layouts/navbar/school_active"%>


<h1> <%=@school.name%></h1>
<p><%= link_to ('<span class="glyphicon glyphicon-chevron-left table-icon" aria-hidden="true"></span>
 Back to all schools').html_safe, school_index_path %></p>
<h2>Departments</h2>
<h4><%= link_to ('<span class="glyphicon glyphicon-plus h4-icon" aria-hidden="true"></span>
 Add department').html_safe, new_department_path(:school_id => @school.id) %></h4>
 <%=render partial: "layouts/tables/department_table", locals: {departments: @departments, show_school_id: false}%>
