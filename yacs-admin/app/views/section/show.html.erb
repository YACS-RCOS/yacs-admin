<h1> <%=@course.name%>: Section <%=@section.name%></h1>
<p><%= link_to ('<span class="glyphicon glyphicon-chevron-left table-icon" aria-hidden="true"></span>
 Back to '+@course.name).html_safe, @course %></p>

<p><%= link_to ('<span class="glyphicon glyphicon-chevron-left table-icon" aria-hidden="true"></span>
 Back to all sections').html_safe, section_index_path %></p>
<h2> Periods</h2>

<table class="table">
  <tr>
    <th>Day</th>
    <th>Start Time</th>
    <th>End Time</th>
  </tr>
<%@section.periods.each do |period|%>
  <tr>
    <td><%=day_to_string(period.day)%></td>
    <td><%=format_time(period.start)%></td>
    <td><%=format_time(period.end)%></td>
  </tr>
<%end%>

</table>